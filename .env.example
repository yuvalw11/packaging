# Environment Configuration Example
# Copy this file to .env and adjust values as needed

# API URL Configuration
# -----------------------------
# For development (local):
# REACT_APP_API_URL=http://localhost:3001/api

# For production with proxy (recommended):
# REACT_APP_API_URL=/api

# For production with direct backend URL:
# REACT_APP_API_URL=http://your-server-ip:3001/api

# Default value if not set: /api

# Server Configuration
# -----------------------------
# SSH/deployment user
SERVER_USER=user

# Server hostname or IP address
SERVER_HOST=127.0.0.1

# Application path on server
SERVER_PATH=/home/user/packing-app

# SSH key path (optional: specify custom SSH key)
SSH_KEY_PATH=~/.ssh/id_rsa

# Docker build architecture
ARCH=linux/amd64
