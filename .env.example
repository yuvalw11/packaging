# Environment Configuration Example
# Copy this file to .env and adjust values as needed

# ========================================
# API Endpoint Configuration
# ========================================
# Where the frontend makes API calls

# For development (local):
# REACT_APP_API_URL=http://localhost:3001/api

# For production with proxy (recommended):
# REACT_APP_API_URL=/api

# For production with direct backend URL:
# REACT_APP_API_URL=http://your-server-ip:3001/api
# REACT_APP_API_URL=https://api.example.com/api

# Default: /api
REACT_APP_API_URL=/api

# ========================================
# Static Files Endpoint Configuration
# ========================================
# PUBLIC_URL controls where static assets (CSS, JS, images) are loaded from
# This affects the <script> and <link> tags in the compiled index.html

# For same origin (default - recommended):
# PUBLIC_URL=

# For CDN or different origin:
# PUBLIC_URL=https://cdn.example.com
# PUBLIC_URL=https://static.example.com

# For subdirectory deployment:
# PUBLIC_URL=/packing-app

# Default: empty (loads from same origin)
PUBLIC_URL=

# ========================================
# Server Configuration (for deployment)
# ========================================
# SSH/deployment user
SERVER_USER=user

# Server hostname or IP address
SERVER_HOST=127.0.0.1

# Application path on server
SERVER_PATH=/home/user/packing-app

# SSH key path (optional: specify custom SSH key)
SSH_KEY_PATH=~/.ssh/id_rsa

# Docker build architecture
ARCH=linux/amd64
